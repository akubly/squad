---
"@bradygaster/squad-cli": patch
"@bradygaster/squad-sdk": patch
---

Migrate read-only command resolution to the structured squad resolver so registered clone checkouts can resolve their host squad, and expose the registry-aware resolver through the SDK root barrel.
