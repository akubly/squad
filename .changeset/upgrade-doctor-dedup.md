---
'@bradygaster/squad-cli': minor
'@bradygaster/squad-sdk': minor
---

`squad upgrade` now updates the global coordinator agent at `~/.copilot/agents/squad.agent.md`. `squad doctor` detects misplaced `.squad/` directories in consumer repos. `upsertEntry` now deduplicates `origins[]` and `clones[]`.
