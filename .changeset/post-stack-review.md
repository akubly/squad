---
"@bradygaster/squad-cli": patch
---

Post-stack adversarial review fixes (piece 21): add `squad status` registry-resolution fields block (`status.ts`), plus EBUSY teardown fix in `cli-entry lifecycle` tests.
