---
"@bradygaster/squad-cli": patch
---

Fold pipeline installs at the repository root and the default generated pipeline discovers callsigns at run time, folding each inbox into its own squad/state/<callsign> branch; --callsign retains the scoped single-callsign pipeline.