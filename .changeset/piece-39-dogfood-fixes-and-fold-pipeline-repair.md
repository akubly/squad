---
"@bradygaster/squad-cli": patch
---

Repair fold-pipeline blockers (embed YAML, git identity, jq quoting, read-tree overlap); fix dry-run direction and allowlist; rename sync flag to --inbox-handle; add git-config handle fallback; gitignore-on-init for allowlist paths.
