# Orchestration Log: CONTROL — 2026-06-05T12:43

**Session:** Adversarial peer review of piece 32 (registry state fields)  
**Route:** TypeScript type-system & build-pipeline validation  
**Mode:** background (Claude Sonnet 4.6)  
**Requested by:** akubly  

## Authorization

Files authorized for read/write:
- `packages/squad-sdk/src/validation.ts` — regex constant and JSDoc (read-only)
- `packages/squad-sdk/src/registry.ts` — RegistryEntry type (read-only)
- `packages/squad-sdk/src/assign.ts` — conditional spread patterns (read-only)
- `packages/squad-sdk/tsconfig.json` — strict mode settings (read-only)
- `packages/squad-cli/tsconfig.json` — CLI tsc environment (read-only)
- `packages/squad-sdk/package.json` — export map and build config (read-only)
- `packages/squad-sdk/dist/validation.{js,d.ts}` — build emit files (read-only)
- `test/**` files mentioned in piece 32 — test coverage validation (read-only)

## Outcome & Verdict

**Verdict:** ⚠️ **APPROVE-WITH-NITS**

### Core findings
- Regex `/^[a-z][a-z0-9-]{1,38}$/` has no flags (`re.flags === ""`); repeated `.test()` calls stateless and consistent
- SDK tsc: exit 0, zero type errors under strict+noUncheckedIndexedAccess
- CLI tsc: exit 2 but all failures pre-existing (stale node_modules); piece 32 introduces zero new type errors
- Build emit: `npm run build` exit 0, both `dist/validation.js` and `dist/validation.d.ts` exist
- Conditional spreads type-sound on both paths; property omitted from object when flag absent
- All 79 SDK tests GREEN

### Ranked nits
1. **NITforward-risk:** Regex accepts trailing/consecutive hyphens (spec-faithful, but forward-risk for piece 33/34 branch-name construction); pieces 33/34 must sanitise at interpolation or tighten regex
2. **NITminor:** Error message hardcodes regex source instead of `DEVELOPER_ALIAS_RE.source` — DRY violation
3. **NITminor:** `validation.ts` JSDoc misplaces field defaults; these belong on `RegistryEntry` field docs, not regex constant
4. **NITcosmetic:** `package.json` indentation inconsistency; `./validation` at 6 spaces while other exports at 4 spaces
5. **NITminor:** Test suite does not assert trailing-hyphen acceptance as explicit contract; coverage gap for forward-risk

**Decision:** No blockers. N1 most consequential for pieces 33/34 branch naming — annotate as forward-risk and defer tightening to piece 33/34 spec decision. N2–N5 are cosmetic/coverage/DRY.

## Completion

- **Time:** 2026-06-05T12:54  
- **Session exit:** clean  
- **Decision drop:** `control-piece-32-adversarial.md` ✓ merged to decisions.md  
