# CONTROL — Piece 35 Orchestration Log

**Session:** Phase B replay — piece 35 fold-pipeline-in-docs-repo  
**Role:** TypeScript Engineer (claude-sonnet-4.6)  
**Mode:** background, read-only  

## Type & Exit-Code Review

Reviewed `packages/squad-cli/src/cli/commands/install-fold-pipeline.ts` and cli-entry.ts dispatch against commit `64eecd47`.

## Five Gates — All PASS

### (a) InstallFoldPipelineOptions signature
✅ PASS — `{ cwd?: string; force?: boolean }` exact match to kickoff. No extra fields, no widened types.

### (b) Platform union tightness
✅ PASS — `platform: 'github' | 'ado'` literal union throughout. `platformDirMap` is `Record<'github' | 'ado', string>`. CFA via `fatal(): never` + double inequality guard narrows `string | undefined` to `'github' | 'ado'`. No type error at call site. Build verified.

### (c) cli-entry.ts dispatch
✅ PASS — Dynamic import uses `.js` extension (ESM-correct). `installFoldPipeline` is correct export. Options `{ cwd: getSquadStartDir() }` structurally valid.

### (d) Exit-code paths
✅ PASS — All four failure modes exit 1 with error messages:
- Missing target directory → exit 1
- Content conflict → exit 1
- Unresolvable docs-repo path → exit 1
- Template not found → exit 1
- Invalid platform arg → `fatal()` before import (never returns)
- No silent exit 0 on any failure path

### (e) Build verification
✅ PASS — `npm run build` errors exclusively in pre-existing files (verified at baseline 763c2451). Zero new type errors in `install-fold-pipeline.ts` or piece-35 cli-entry.ts additions (lines 1452–1465).

## Registry API Note

✅ Implementation uses `registry?.squads.find(...)` (correct field). Does NOT use non-existent `.entries` field. Registry-first topology preserved.

## Outcome

**APPROVE** — All five gates passed. InstallFoldPipelineOptions well-typed. Platform union tight. CFA narrowing via fatal():never valid. All exit paths to process.exit(1). Zero new build errors. Ready for Phase C.

---

*Logged by Scribe at 2026-06-07T06:44:08Z*
