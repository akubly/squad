# GNC — Piece 19 Security Guards Revision (Node.js Runtime)

**Timestamp:** 2026-05-19T22:30:35Z  
**Agent:** GNC (Node.js Runtime, claude-sonnet-4.6)  
**Role:** Runtime Security Implementation  
**Branch:** `akubly/upstream-19-copilot-payload`  
**Commit:** `2377c3a8` (amended, final)

## Verdict

✅ **REVISION COMPLETE** — Security guards implemented, EECOM locked out per protocol.

## Work Produced

### 1. Symlink-Safe `_copyDirRecursive`

Implemented symlink detection:
- Checks `lstat` for symlink before following.
- Raises `ERR_PAYLOAD_IO` if symlink detected.
- Prevents file exfiltration on cold-start trust boundary.

### 2. `assertValidCallsign` Helper

New validation function:
- Pattern: `^[A-Za-z0-9_-]+$` (max 64 chars)
- Prevents path traversal via `..` in callsign argument.
- Mirrors piece-02 callsign format constraint.

### 3. New Error Code

`ERR_PAYLOAD_INVALID_CALLSIGN`: Raised when callsign fails validation. Surfaces as `warnings[]` entry (post-write scenario).

### 4. Test Coverage

26 new tests added:
- Symlink detection edge cases
- Callsign validation (hyphens, underscores, invalid chars, length limits)
- Integration with cold-start load sequence

## Blocking Gaps Remaining

FIDO test coverage gaps + CAPCOM contract wrapping — not in GNC scope.

## Handed Off To

CAPCOM (Round 2) for error surface wrapping + FIDO test gap fixes.

## Noteworthy

EECOM locked out per Reviewer Rejection Protocol. GNC sole implementer of revision. No collaboration with original author.
