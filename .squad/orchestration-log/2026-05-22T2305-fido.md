# FIDO Re-Verdict — Piece 22 (2026-05-22T23:05Z)

**Branch:** `squad/piece-22-unify-doctors`  
**Revision commit:** `78297559`  
**Role:** Flight Dynamics Officer (Quality Gate Authority)  

## Session Summary

FIDO performed re-verdict review on CONTROL's revision of piece 22 after initial rejection. Both blockers resolved; all gates green. Verdict: ✅ APPROVE.

## Resolution Verification

### Blocker 1: warn→stderr routing — VERIFIED FIXED
**Evidence:**
- `renderFinding()` at doctor.ts:780–802 routes error/warn to `console.error`
- `info` correctly uses `console.log`
- Old `_renderFinding` in cli-entry.ts (which used `console.log` for all) is fully removed
- Comment at cli-entry.ts:1077: `// Warnings go to stderr.` now matches code

**Test validation:** Test would fail on ef09d3d3 (old code) because stderrSpy would get 0 calls (all went to stdout).

### Blocker 2: cross-source escalation test — VERIFIED FIXED
**Evidence:**
- Test at doctor.test.ts:545: system-warn + registry-error scenario
- Asserts: `stderrSpy.toHaveBeenCalledTimes(2)`, stdout never called, `deriveExitCode(findings) === 2`
- Companion test at line 564: info findings go to stdout only
- Both tests would fail on ef09d3d3 because old code used `console.log` for all severities

**Test discovery:** Ran against mental model of old code — stderrSpy would receive 0 calls, assertions would fail. Confirms test catches the bug.

### Directive N1: exhaustive exit-code helper — APPROVED
- `deriveExitCode()` at doctor.ts:809–824 uses switch with never default
- cli-entry.ts:1072 delegates to helper (old inline `.filter()` removed)
- Both renderFinding and deriveExitCode carry identical exhaustive switches

### Directive N3: readonly fields — APPROVED
- doctor-types.ts: all DoctorFinding fields marked readonly ✅

### Directive N4: import alignment — APPROVED
- Static `DoctorFinding` import from doctor-types.js removed from cli-entry.ts ✅
- `renderFinding`/`deriveExitCode` destructured from dynamic import of doctor.js ✅

## Gate Results

| Gate | Status |
|------|--------|
| Build | ✅ CLEAN |
| Lint | ✅ CLEAN |
| Doctor tests | ✅ 53/53 GREEN |
| LOC budget | ✅ 159/200 net |
| State hygiene | ✅ No .squad/ leaks |

## Lockout Records

- **Flight:** 🔒 Locked out (original author, did not fix both blockers before reassign)
- **CONTROL:** ✅ Cleared — revision approved

## Verdict

✅ **APPROVED** — piece 22 unification cleared. Cycle resolved. Two commits on branch (ef09d3d3 Flight + 78297559 CONTROL). Awaiting local Brady review/merge.
