# Flight Orchestration — Q5: Scrub-Gate Evolution

**Date:** 2026-05-29  
**Agent:** flight-6  
**Query:** Q5 — How should scrub-gate rules be extended across pieces 26–30?

**Outcome:** ✅ RESOLVED

**Decision:** Hybrid approach: per-piece behavioral test files validate piece-internal contracts (Q4 pattern). A single post-30 gate-audit piece (30.5) adds genuinely cross-cutting gate rules in one atomic commit AFTER all surfaces 26–30 are known. Pieces 26–28 do NOT modify `_scrub-gate.ps1`.

**Three new gate rules for piece 30.5:**  
1. No `Users\` or `/home/` path segments in `.squad/config.json` or `.squad/publish-metadata.json`
2. ADO variable syntax `$(...)` exclusion in `.squad-templates/ado/**`
3. Alias format validation `[a-z][a-z0-9-]{0,38}` for `developerAlias`

**Rationale:** Piecemeal gate extensions create regression coupling. Hybrid keeps gate evolution atomic and avoids mid-arc breakage. New test fixture in piece 30.5: `test/scrub-gate-surfaces.test.ts`.

**Duration:** Single lead deep-dive session. Option C chosen. Piece 30.5 introduced.
