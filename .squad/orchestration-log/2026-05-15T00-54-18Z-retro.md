# Orchestration Log: RETRO — 08c Security & Lifecycle Review

**Timestamp:** 2026-05-15T00:54:18Z  
**Agent:** RETRO (Security/Lifecycle)  
**Session:** Phase B piece 08c adversarial review  
**Branch:** `akubly/upstream-08c-migrate-lifecycle-commands` @ `3c2528d4`  

## Spawn Manifest

**Type:** general-purpose  
**Mode:** background  
**Model:** gpt-5.5 (analytical diversity for security focus)  

**Why:** Security & lifecycle review focused on long-running process risks  
**Inputs Read:**
- spec branch
- commit 3c2528d4
- Scribe state commit 8bed2884
- lifecycle source files
- decisions.md
- retro history

**Files Produced:**
- .squad/decisions/inbox/retro-08c-review.md
- .squad/skills/lifecycle-fail-closed-review/SKILL.md (new)

## Verdict

**REJECT** — Blocker: Lifecycle commands do not fail-closed on stale/invalid registry-resolved squad paths.

## Critical Finding

**Severity:** blocker  
**What:** `packages/squad-cli/src/cli-entry.ts:872-889` and `:1088-1097` pass `resolved.path` directly into lifecycle runners; resolver returns clone/origin registry paths without existence check (resolution-v2.ts:395-399, :426-430).

**Why:** `start` and `rc` create long-running bridges, tunnels, PTYs, child processes. If stale/user-selected registry maps checkout to missing or unintended squad path, command still starts session with incorrect bridge metadata and roster. Ambiguous matches surfaced, but stale single matches silently accepted.

**Risk:** Long-lived process launched with wrong squad context; undetected roster/bridge metadata corruption.

## Secondary Finding

**Severity:** major  
**What:** Malformed registry content during clone/origin matching silently ignored; lifecycle spec requires failures to surface before long-running state created.

**Why:** Test (legacy-resolver-migration.test.ts:637-648) describes "resolver-throw path" but only proves command exits quickly in isolated fixture. Real fallback squad could mask bad registry.

**Risk:** Missing registry errors hidden by fallback resolution; no user feedback on root cause.

## Recommended Fixes

1. Add existence check to resolver (fail-closed pattern) — validate resolved squad path is existing directory before lifecycle runner invocation
2. Parse/read failures on explicit registry should be fatal for lifecycle command resolution
3. Add fall-back-squad fixture to malformed-registry test so bad registry content cannot silently select another squad
4. Update lifecycle tests to prove stale/invalid paths do not start bridge

## Confirmed Clean

- Subprocess CWD not conflated with resolved squad path ✓
- Ambiguous clone/origin matches throw instead of silent pick ✓
- New lifecycle fixtures cleaned with recursive removal ✓
- No committed credentials or personal data ✓

## Revision Owner

**Recommended:** GNC (General-Purpose Connector)  
**Rationale:** GNC has deep resolver and registry validation context from prior pieces; this is a resolver-layer fix, not CLI interface. VOX and EECOM locked out per Reviewer Rejection Protocol.

---

**Status:** REJECTED, revision assigned to GNC, integrated into 2026-05-15 adversarial review batch
