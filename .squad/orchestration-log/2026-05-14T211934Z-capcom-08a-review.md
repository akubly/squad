# Orchestration Log: CAPCOM 08a Review

**Timestamp:** 2026-05-14T21:19:34Z  
**Agent:** CAPCOM (SDK Expert)  
**Session:** Phase B piece 08a adversarial review  
**Model:** claude-sonnet-4.6  

## Lens

SDK public API shape, CLI↔SDK boundary discipline, subpath import hygiene, dispatch consistency.

## Verdict

REJECT

## Key Findings

- **Boundary violation 1:** Three CLI files import `resolveSquad` from `@bradygaster/squad-sdk/resolution-v2` subpath instead of SDK root barrel `packages/squad-sdk/src/index.ts`
- **Boundary violation 2:** Dispatch inconsistency — `cli-entry.ts` and `config.ts` use v2 resolver; `cross-squad.ts` uses it for discover; but `delegateCommand` still calls legacy `detectSquadDir(process.cwd())`
- No SDK source changes in commit; scope stayed in CLI/test/state
- No unsafe boundary casts detected (no `any`, `@ts-ignore`, `as unknown as`, non-null assertions)

## Outcome

Piece 08a rejected. Boundary discipline violation is definitive. Revision scope: export stable registry-aware resolver through SDK root barrel, update all three CLI dispatch paths to consume unified surface, remove legacy fallback divergence in delegateCommand.

## Revision Owner

CAPCOM (self-nominated, coordinator accepted). Fix is SDK domain responsibility.
