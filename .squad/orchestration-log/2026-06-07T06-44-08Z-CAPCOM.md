# CAPCOM — Piece 35 Orchestration Log

**Session:** Phase B replay — piece 35 fold-pipeline-in-docs-repo  
**Role:** SDK Expert (claude-haiku-4.5)  
**Mode:** background, read-only  

## Review Scope

SDK/registry contract verification for `packages/squad-cli/src/cli/commands/install-fold-pipeline.ts` against commit `64eecd475605a8f9cc1d6f9707d6ae72f055d59a`.

## Critical Checkpoints

### (a) Import paths match sync.ts EXACTLY
✅ PASS — Byte-for-byte identical:
- `import { loadRegistryFromDisk } from '@bradygaster/squad-sdk/registry';`
- `import { normalisedPathKey } from '@bradygaster/squad-sdk/path-utils';`

### (b) RegistryEntry field names VALID
✅ PASS — All accessed fields in schema:
- `entry.path` (required, string)
- `entry.clones` (optional, string[])
- `entry.clones?.some()` with `normalisedPathKey()` comparison — correct
- No invented fields, no references to non-existent `docsRepoPath` on entry

### (c) path.dirname(entry.path) derivation matches runSyncStatus
✅ PASS — Verbatim pattern from sync.ts lines 523–529:
- `loadRegistryFromDisk()` call
- `normalisedPathKey()` normalization on repoRoot
- `registry?.squads.find(e => e.clones?.some(...))`
- Derivation via `path.dirname(entry.path)` on match

### (d) Registry-first preserved, config.json fallback only
✅ PASS — Correct topology:
- Registry-first phase (lines 62–69): immediate `docsRepoPath = path.dirname(entry.path)` if entry found
- Fallback phase (lines 71–82): config.json `stateLocation` accessed ONLY if `!docsRepoPath`
- Comment explicitly labels fallback phase as "for unregistered/single-repo contexts"

## Outcome

**APPROVE** — Commit `64eecd475605a8f9cc1d6f9707d6ae72f055d59a` passes all SDK contract checkpoints. Import paths exact. RegistryEntry fields valid. path.dirname pattern matches. Registry-first semantics preserved. No changes requested.

---

*Logged by Scribe at 2026-06-07T06:44:08Z*
