# CAPCOM — Piece 19 SDK Contract Review (SDK Expert)

**Timestamp:** 2026-05-19T22:30:35Z  
**Agent:** CAPCOM (SDK Expert, claude-sonnet-4.6)  
**Role:** Adversarial SDK Contract & Surface Reviewer  
**Branch:** `akubly/upstream-19-copilot-payload`  
**Commit:** `2377c3a8` (amended, final)

## Verdict

❌ **REJECT** — 3 blocking contract and security issues identified.

## Blocking Issues

1. **`CopilotPayloadError` surface leak:** Exception leaks past `AssignError` wrapper in `runAssign` path. Callers expect `AssignError | success`, receive `CopilotPayloadError`. Contract violation.
2. **`_copyDirRecursive` symlink vulnerability:** Implementation follows symlinks in payload dirs. File exfiltration risk on cold-start trust boundary (caller-provided payload dir could contain symlinks pointing outside source tree).
3. **Callsign format guard missing:** No validation before `path.join(payloadDir, callsign)`. `path.join` normalizes `..` components — attacker-supplied callsign could escape bounds.

## Work Produced

Security audit report + blocking issue list (for GNC + CAPCOM Round 2 revision).

## Resolution Path

- GNC: Implemented symlink-safe copy + `assertValidCallsign` guard + new `ERR_PAYLOAD_INVALID_CALLSIGN` error code (26 tests added).
- CAPCOM (Round 2): Wrapped error surface in both paths + fixed FIDO gaps.

## Handed Off To

GNC (Runtime) for security guard implementation.
