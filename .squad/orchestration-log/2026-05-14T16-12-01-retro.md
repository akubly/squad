# RETRO — Piece 08b Security Review

**Date:** 2026-05-14T16:12:01Z  
**Piece:** 08b — Migrate user-action commands to resolveSquad() precondition  
**Branch:** `akubly/upstream-08b-migrate-user-action-commands` @ commit `012d6d16`  
**Verdict:** APPROVE WITH NITS

## Role

Security gate. Audits PII/secret leakage, path traversal risk, symlink/template injection, and test fixture isolation.

## Scan Results

### PII / Secret Scan: CLEAN
- Scan pattern: `email|token|@.*\.com|ghp_|github_pat_`
- Only match: standard git `Author:` and `Co-authored-by:` metadata
- No credentials, no email addresses in source or `.squad/` artifacts

## Risk Inventory

| # | Severity | File:Line | Finding | Mitigation |
|---|----------|-----------|---------|------------|
| 1 | LOW | `assign.ts:81-82,129` | `--home` flows into `path.join(home, ...)` without absolute-path validation | Add `path.isAbsolute(home)` guard before install |
| 2 | LOW | `assign.ts:108` | `path.normalize(opts.cwd)` doesn't guarantee absolute result; relative paths in registry confuse resolver | Add `path.isAbsolute(opts.cwd)` check before registry write |
| 3 | LOW | `cli-entry.ts` | `consult --status` bypasses resolver guard (by design); read-only but verify no write locks | Confirm `runConsult --status` never acquires write lock |
| 4 | LOW | `legacy-resolver-migration.test.ts:44-68` | `runCli` spreads `process.env` without suppressing `XDG_CONFIG_HOME`/`APPDATA`; fixture collision risk | Add env overrides to resolver-guard tests |

## Confirmed-Clean Vectors

✅ **Credential leakage:** No `console.log` of env vars, remotes, or config objects  
✅ **Symlink defense:** `_installCoordinatorAgent` uses `lstatSync`, unlinks pre-existing symlinks  
✅ **Template source injection:** `getTemplatesDir()` bounds path to 6 levels, no user control  
✅ **Two-phase guard:** `resolveSquad()` fires before all side effects in both `assign.ts` and dispatch  
✅ **Test fixture cleanup:** `beforeEach`/`afterEach` use randomized root with fake `.git`  

## Status

APPROVE WITH NITS. No CRITICAL/HIGH findings. Four LOW hardening candidates noted; none block merge. FIDO rejection makes this moot for current cycle.
