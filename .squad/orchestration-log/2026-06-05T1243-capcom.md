# Orchestration Log: CAPCOM — 2026-06-05T12:43

**Session:** Adversarial peer review of piece 32 (registry state fields)  
**Route:** SDK contract & export surface validation  
**Mode:** background (Claude Sonnet 4.6)  
**Requested by:** akubly  

## Authorization

Files authorized for read/write:
- `packages/squad-sdk/src/registry.ts` — RegistryEntry interface (read-only)
- `packages/squad-sdk/src/validation.ts` — DEVELOPER_ALIAS_RE constant (read-only)
- `packages/squad-sdk/src/assign.ts` — warm/cold path preservation logic (read-only)
- `packages/squad-sdk/package.json` — export map structure (read-only)
- `packages/squad-sdk/dist/validation.{js,d.ts}` — build emit verification (read-only)
- `test/**` files mentioned in piece 32 — test coverage validation (read-only)

## Outcome & Verdict

**Verdict:** ⚠️ **APPROVE-WITH-NITS**

### Core findings
- Re-assign preservation mechanism validated on warm and cold paths: `...entry` spread precedes conditional overwrites, existing values survive when options absent
- Test P32.A5 confirmed as genuine disk round-trip (writeRegistry → loadRegistryFromDisk → validate)
- Export contract integrity: `./validation` subpath present in package.json, both dist files exist
- `DEVELOPER_ALIAS_RE` import from subpath is correct; circular dependency structurally impossible

### Ranked nits
1. **MEDIUM — maintenance risk:** `package.json` brace indentation broken around `./validation` entry; valid JSON but visually suggests nesting
2. **MEDIUM — spec defect:** `INVALID_ALIAS` error code breaks `ERR_ASSIGN_*` convention; spec-binding, recommend spec errata to rename `ERR_ASSIGN_INVALID_ALIAS` before piece 33/34 adoption
3. **LOW — coverage gap:** No test for cold-start re-assign preservation with existing `stateRemote`; code is correct, test is missing
4. **LOW — doc placement:** `validation.ts` JSDoc mentions `'origin'`/`'squad-state'` defaults; these are piece 33 consumer semantics, not validation-module concern

**Decision:** No blockers. Nits 1 and 3 highest-value for piece 33/34 foundation. Nit 2 track as spec errata with zero-cost rename opportunity before downstream adoption.

## Completion

- **Time:** 2026-06-05T12:54  
- **Session exit:** clean  
- **Decision drop:** `capcom-piece-32-adversarial.md` ✓ merged to decisions.md  
