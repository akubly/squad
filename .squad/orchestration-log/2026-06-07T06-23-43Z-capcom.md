# Orchestration Log: CAPCOM — Piece 34 (Phase B Replay Session)

**Agent:** CAPCOM (SDK Expert)  
**Role:** Reviewer — SDK contract compliance  
**Session:** Piece 34, Phase B replay  
**Mode:** Background (async)  
**Date:** 2026-06-07T06:23:43Z  

## Scope

SDK-contract adversarial review of commit `b0045b27` (client-side publish triggers).

## Review Findings

### 1. Registry-First Resolution (PASS)

Both `runSync` and `runSyncStatus` resolve `teamRoot` exclusively from registry (`loadRegistryFromDisk` → `path.dirname(entry.path)`). config.json used only in fallback else-branch (no registry match). Constraint met: no regression to config.json-primary topology.

### 2. `installCrossRepoHook` Signature & Call-Site (PASS)

Export signature `installCrossRepoHook(docsRepoPath, options?)` correct. Warm-path derives `docsRepoPath` from registry entry, not config.json. Failure mode: warning push, not throw. Injectable seam `_installCrossRepoHookFn` (@internal, optional, defaults to real implementation) matches `_writeRegistryFn` pattern from piece 32.

### 3. HookPipeline Non-Conflation (PASS)

`packages/squad-sdk/src/hooks/index.ts` (HookPipeline) not referenced in any changed file. Sub-proposal B cleanly deferred: TODO stub in install-hooks.ts, decision record, skip-annotated test. No HookPipeline as stand-in.

### 4. `.last-publish` Write Points (PASS)

Written after `publishTeamRootToInbox` resolves (cross-repo sync) and after `syncPush` returns (single-repo sync). Best-effort wrapper prevents aborting successful sync. `.squad/.last-publish` added to `PUBLISH_ALLOWLIST_EXACT`. Piece-33 push contract preserved.

## Verdict

**APPROVE** — no changes requested. All contract checks pass. Registry-first topology and deferral pattern correctly implemented.

## Outcome

SDK contract review complete and approved.
