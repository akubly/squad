# Orchestration Log: Control (Revision Agent - Piece 31 Test Timeout Fix)

**Session:** piece 31, quality gate remediation  
**Timestamp:** 2026-06-03T22:30:46Z  
**Agent:** control-1  
**Outcome:** ✅ Complete

## Assigned Failures

Two timeout failures in `test/cli/cross-repo-sync.test.ts` identified by FIDO:
1. Line 155: hydrate `it()` — no explicit timeout, exceeds 5000ms default
2. Line 215: metadata `beforeAll()` — no explicit timeout, exceeds 10000ms default

## Fixes Applied

**Line 155:**
```typescript
it('third clone can hydrate from a synthetic squad-state commit on the remote', async () => {
  ...
}, 30_000);  // Added explicit 30s timeout
```

**Line 215 (beforeAll context):**
```typescript
beforeAll(async () => {
  ...
}, 30_000);  // Added explicit 30s timeout
```

Both git-operation-heavy test sequences now complete cleanly within the 30s window.

## Test Results

Both tests pass after fix. Full piece-31 test file suite now green.

## Blocking Conditions

None. EECOM implementation and test updates complete.
