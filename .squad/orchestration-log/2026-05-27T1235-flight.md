# Orchestration — Flight 2026-05-27T12:35

**Agent:** Flight (Lead)  
**Mode:** background  
**Branch:** N/A (spec authoring, not implementation)

## Outcome

Piece-24 scope decision + spec + handoff written.

### Scope Decision

**Selected items:** D-6, D-8, D-9, D-16 (SDK adapter + OTel typing cluster)  
**Rejected items:** D-14 (feature/implementation work; unmet prerequisite), D-18 (breaking SDK change; piece 25), D-10/D-17 (won't fix)

| Item | Severity | Description | LOC Impact |
|------|----------|-------------|-----------|
| D-6  | M | Remove 4 `@typescript-eslint/no-explicit-any` from adapter/client.ts; introduce `CopilotSessionLike` interface | ~15 LOC |
| D-8  | M | Remove blanket `any` from runtime/otel-api.ts; type no-op OTel shims | ~25 LOC |
| D-9  | S | Remove 3 inline `any` from runtime/otel.ts; type dynamic OTel SDK constructor variables | ~20 LOC |
| D-16 | S | Rename `AgentHandleImpl.markIdle()` → `setIdle()`; remove misleading `@internal` | ~3 LOC |

**Total estimated net:** ~51 LOC (+76 added, −25 removed)  
**LOC budget:** 200 production LOC  
**Headroom:** 149 LOC ✅

### Package Impacted

`@bradygaster/squad-sdk` — patch bump required. No CLI package changes.

### Key Decisions

1. **D-14 deferred:** Span propagation is feature/implementation work with conditional prerequisite ("when agent lifecycle spans are complete"). Not binding for piece 24. Deferred to dedicated follow-up (24b or 25+).

2. **Heuristic applied:** Mixed-type audit clusters (cleanup + feature) must be split by type. Feature items defer with explicit prerequisite language. Updated guidance in ship-debt-selection skill.

3. **CONTROL N2 and Directive 2 not binding:** N2 (source-grouping exhaustiveness) deferred to next CLI piece. Directive 2 (resolver env seam) explicitly filed for piece 25 in decisions.md.

### Tensions Reviewed

- **Piece 23 in-flight:** Zero structural overlap. Piece 23 touches only CLI files; piece 24 touches only SDK files (`packages/squad-sdk/src/`). Clean stacking from piece 23's tip regardless of EECOM rev status.

### Risk Flagged

**`_noopTracer.startActiveSpan` variadic rest-arg:** May require `eslint-disable-next-line` when typing the no-op implementation. Single targeted disable acceptable per team policy. Monitor during implementation.

## Spec & Handoff

- **Spec:** `docs/proposals/piece-24-sdk-adapter-otel-typing.md`
- **Handoff:** session-state handoff file (for implementer — CAPCOM recommended)

## Next

Awaiting piece-24 kickoff (after Brady's piece-23 approval). Implementation expected to pick up CAPCOM's SDK expertise from piece 22 revision.

---

**Scribe status:** Decision merged into decisions.md, handoff artifacts prepared.
