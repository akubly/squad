# Orchestration Log — Piece 29 Nit Revision (Flight)

**Timestamp:** 2026-06-02T21:16:49Z  
**Agent:** Flight (Lead)  
**Session:** Piece 29 nit revision (post-adversarial-review fix-up)  
**Procedures Status:** Locked out (original piece-29 author)

## Revision Rationale

Procedures locked out per normal protocol. Flight selected as revision author per piece-27 precedent (same-branch revision pattern). Mandatory nits from consolidated adversarial-review verdict (APPROVE-WITH-NITS) required immediate fix-up.

## Nits Resolved

**M1 (Flight N1 + PAO N1 convergent):** Explore-agent spawn pattern updated to carry all five mandatory variables (TEAM_ROOT, WORK_ROOT, STATE_REMOTE, STATE_BRANCH, DEVELOPER_ALIAS) with consistent `KEY: value` formatting. Old `TEAM ROOT:` (space, no underscore) format eliminated across all spawn contexts.

**M2 (Flight N2):** Added "WORK_ROOT resolution procedure" paragraph documenting actual SDK semantics. `loadDirConfig()` → `teamRoot` field → resolved relative to WORK_ROOT. Absent config defaults to TEAM_ROOT == WORK_ROOT fallback. Resolution chain explicit; no guessing at Coordinator level.

**M3 (PAO N2):** Added "Single-repo case" callout. When TEAM_ROOT == WORK_ROOT (mono-repo default), four path variables collapse to two distinct values. All write rules remain in effect. Coordinator must still pass both variables in spawn prompts despite identity.

## Revision Commit

- **SHA:** b7ff4f99 (atop b642f9cd)
- **Files changed:** 6 (canonical template + 4 mirrors + 1 test file)
- **Test delta:** +45 new presence assertions (80 → 125 total in team-root-work-root-protocol.test.ts). template-sync.test.ts unchanged (160/160).
- **Scrub-gate baseline:** Zero new violations vs parent. Gate 1 FAIL + Gate 3/4 WARN are pre-existing per prior decisions.

## Push Confirmation

Branch `squad/piece-29-team-root-work-root-protocol` pushed to origin. Revision ready for downstream Phase C (user PR open decision).

## Test Status

All 223 assertions green:
- team-root-work-root-protocol.test.ts: 125/125 ✓
- template-sync.test.ts: 160/160 ✓

Mutation-test pass: both M1 and M3 nit assertions catch intentional regressions. Baseline reconfirmed.
