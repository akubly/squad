# Flight Orchestration — Q4: Piece 30 YAML Validation

**Date:** 2026-05-29  
**Agent:** flight-5  
**Query:** Q4 — How should piece 30 YAML templates be validated?

**Outcome:** ✅ RESOLVED

**Decision:** Piece 30 adds `test/cli/ado-templates.test.ts` with behavioral assertions (TDD pattern) using the `yaml` npm devDependency. Tests validate piece-specific contracts: no `pr:` trigger on `publish-inbox.yml`; `squad/inbox/*` trigger on `fold-squad-state.yml`; no broad OAuth scope; alias-empty guard. NO schema vendoring.

**Rationale:** TDD discipline requires tests on every shipped surface. Spec contracts (our concerns) are testable in-repo; ADO schema conformance is best caught by Phase C reviewer. Schema vendoring (180KB) violates replay isolation with no proportional value.

**New devDependency:** `yaml` (lightweight, no transitive deps).

**Duration:** Single lead deep-dive session. Option D chosen.
