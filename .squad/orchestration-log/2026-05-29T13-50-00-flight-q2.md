# Flight Orchestration — Q2: sourceWorkRoot Structure

**Date:** 2026-05-29  
**Agent:** flight-2  
**Query:** Q2 — How should sourceWorkRoot be shaped in publish-metadata.json?

**Outcome:** ✅ RESOLVED via RETRO follow-up

**Decision:** `sourceWorkRoot` is an object: `{ repo: <basename>, pathHash: "sha256:<hex>" }`. Raw paths MUST NOT appear in published metadata. Hashed form preserves cross-clone race-debugging signal at zero runtime cost.

**Security Challenge:** PII leakage risk in pipeline publish path. Resolved via RETRO agent deep-dive on identity floor and infrastructure surface minimization.

**Duration:** Lead deep-dive + RETRO security challenge. See retro-q2-challenge log.
