# Orchestration Log: CONTROL — Piece 07 API Surface Review

**Spawn DateTime:** 2026-05-14T12:09:13.633-07:00  
**Agent:** CONTROL (claude-sonnet-4.6)  
**Session:** Phase B piece 07 adversarial review round (post-implementation)  
**Commit:** `a1e82411` — `akubly/upstream-07-register-merge-clones-origins`

## Verdict

**REJECT**

## Blocking Issues

### BLOCKER-1: Help Text Contract — `--path` Shown as Required

**File:** `packages/squad-cli/src/cli-entry.ts`, lines 245, 248

Usage block shows `--path` as `(required)`. In piece 07, `--path` is now optional when the callsign already exists or can be inferred from git root.

Users reading `squad register --help` receive false contract information.

**Fix:** Update line 245 to show `--path` optional (e.g., `[--path <dir>]`). Update line 248 to remove `(required)` or change to `(optional — inferred from Git root when omitted)`.

### BLOCKER-2: Missing Flag Documentation in Help

**File:** `packages/squad-cli/src/cli-entry.ts`, lines 243–252

Help text for `register --help` does not mention `--origin <url>` or `--clone <path>`. These are new public CLI flags shipped in piece 07. They are undiscoverable via help.

**Fix:** Add help lines for `--origin` and `--clone`.

## Non-Blocking Issues

- **[NIT-1]** Inconsistent string concatenation in 'merged' log line (line 915) — should use template literal like peers
- **[NIT-2]** Inconsistent mutation vs. spread-copy patterns in append modes
- **[NIT-3]** Dead-code `'already-active'` in `RunRegisterOutcome` union — pre-existing from piece 06

## Revision Assignment

CONTROL self-nominated for revision, accepted. CONTROL owns CLI parser surface per charter. Both blockers are pure text changes to help block; no risk of runtime regression.
