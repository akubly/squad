# Flight — Piece 08b Architectural Review

**Date:** 2026-05-14T16:12:01Z  
**Piece:** 08b — Migrate user-action commands to resolveSquad() precondition  
**Branch:** `akubly/upstream-08b-migrate-user-action-commands` @ commit `012d6d16`  
**Verdict:** APPROVE WITH NITS

## Role

Architectural lead. Assesses implementation correctness, pattern consistency, and compound design debt.

## Key Assessment

The implementation correctly gates all three user-action commands behind resolver preconditions before side effects execute. Transactionality contract is honored across all paths. No correctness bugs identified.

## Nits Identified

### Finding 1 — Dual Resolver Functions (Medium)
- `consult`/`link` use `resolveSquadV2()` from SDK subpath
- `assign-to-copilot` uses `resolveSquad()` from SDK top-level
- Maintenance risk: grep for usage patterns hits two symbols

### Finding 2 — Guard Location Inconsistency (Medium)
- `consult`/`link` guards at dispatch in `cli-entry.ts`
- `assign` guard inside `assign.ts` module boundary
- Pattern inconsistency creates friction for future contributors

### Finding 3 — Resolved Data Discarded at Dispatch (Low)
- Guard computes `resolved`, uses only as boolean gate
- If runners re-resolve independently, guard+runner can diverge
- Acceptable for this piece but weaker pattern than spec envisions

### Finding 4 — `assign` Alias Added (Nit)
- Net-new or existing? Needs documentation in command surface inventory

### Finding 5 — `--status` Exemption (Correct)
- Query-only exemption is spec-compliant and tested

### Finding 6 — Coordinator Install Coupling (Correct)
- Best-effort install with escape hatch is correct scope

## Recommendation

Harmonize dual-resolver import and align guard-location pattern before 08c lands. Non-blocking for 08b.

## Status

NITS NOTED. Piece sound for approval. FIDO rejection makes this moot for current cycle.
