# FIDO Orchestration Log — 2026-06-08

**Agent:** FIDO (Quality Owner)  
**Session ID:** akubly/upstream-npm-release

## Tasks Executed

1. **Read-only bug audit of shared-squad dotfile flow (pieces 32–35)**
   - Ran build gate: `npm run build` (SKIP_BUILD_BUMP=1) → exit 0 ✅
   - Ran test gate: 6 test suites (52 tests total, 26 PASS, 26 FAIL)
   - Root cause: BUG-1 — test mocks target stale @bradygaster scope while source code uses @wifi-aware scope.

## Critical Findings (Blockers)

**BUG-1 (BLOCKER):** Test mock scope mismatch — all registry mocks dead  
- 4 test files have `vi.mock('@bradygaster/squad-sdk/...')` but source imports `@wifi-aware/squad-sdk/...`
- 22 test failures due to empty @bradygaster in node_modules
- Blocks dogfooding and test gate
- Fix: Replace @bradygaster→@wifi-aware in sync-registry-resolution.test.ts, cross-repo-sync.test.ts, sync-command.test.ts, install-fold-pipeline.test.ts

**BUG-2 (BLOCKER):** publishTeamRootToInbox allowlist guard throws on real docs-repo  
- Guard rejects standard .squad/ files (team.md, agents/, routing.md, config.json)
- Test fixtures only use allowlisted files — real scenario untested
- Blocks dogfooding (squad sync --push always fails)
- Fix: Change from BLOCK to FILTER — skip non-allowlisted files, only publish allowlisted subset

**BUG-3 (BLOCKER):** --developer-alias silently dropped from squad assign  
- Flag implemented in handler but not wired in CLI dispatch (3-stage break in assign-args.ts + cli-entry.ts)
- Blocks dogfooding (auto-publish hook never installs)
- Fix: Add to NAMED_FLAGS, parseAssignArgs, destructuring, runAssign call

## High-Severity Findings (Should-Fix)

**BUG-4 (HIGH):** --skills-from parsed but not forwarded (cli-entry.ts:1309 missing destructure)  
**BUG-5 (HIGH):** --state-remote / --state-branch not in NAMED_FLAGS (same 3-stage break as BUG-3)  

## Medium-Severity Findings

**BUG-6 (MEDIUM):** --dry-run missing from squad sync --help output  
**BUG-7 (MEDIUM):** hydrateTeamRootFromStateRef idempotency guard structurally broken (HEAD vs orphan SHA)  

## Low-Severity Finding

**BUG-8 (LOW):** Coverage gap — no test with realistic docs-repo structure (team.md + agents/ present)

## Outcomes

- Pre-dogfooding audit complete; 3 blockers identified + ranked.
- BUG-1 must be fixed in test layer before any surface testing.
- BUG-2, BUG-3 must be fixed in source layer before dogfooding.
- BUG-4, BUG-5, BUG-6, BUG-7, BUG-8 documented for next piece or polish pass.
