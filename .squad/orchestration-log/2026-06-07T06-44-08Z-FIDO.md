# FIDO — Piece 35 Orchestration Log

**Session:** Phase B replay — piece 35 fold-pipeline-in-docs-repo  
**Role:** Quality (claude-sonnet-4.6)  
**Mode:** background, read-only  

## Verification Scope

Adversarial validation of commit `64eecd475605a8f9cc1d6f9707d6ae72f055d59a` against spec requirements.

## Test Evidence

| Suite | Result |
|---|---|
| fold-github.test.ts (10 tests) | 10/10 PASS |
| fold-ado.test.ts (10 tests) | 10/10 PASS |
| install-fold-pipeline.test.ts (7 tests) | 7/7 PASS |
| install-hooks.test.ts (piece-34 regression) | 6/6 PASS, 1 pre-existing SKIP |
| sync-command.test.ts (piece-34 regression) | 6/6 PASS |

**Total new piece-35 tests: 27/27 GREEN**

## Assertion Quality Findings

All spec-required assertions validated as REAL (parsed object, not raw grep):
- ✅ `not.toHaveProperty('pull_request')` / `not.toHaveProperty('pr')` — parsed YAML object
- ✅ `permissions.contents === 'write'` — parsed YAML object
- ✅ `allowScripts` absence at pool level — parsed YAML object
- ✅ Single-writer invariant comment — raw string (correct; YAML parser strips comments)
- ✅ `--force-with-lease` — raw string (correct; bash content is YAML string value, not parsed key)

## Registry-First Correctness

- ✅ D7: Pre-writes wrong `stateLocation` in config.json; verifies registry-derived path wins; verifies wrong path NOT written
- ✅ Implementation uses `registry?.squads.find(...)` (correct field; kickoff example used stale `.entries`)

## Binding Standards Reaffirmed

For piece 35 and forward:
1. **YAML comment assertions:** Must use `raw.toContain()` — YAML parser strips comments from parsed object; this is ONLY valid assertion
2. **Registry-first adversarial test pattern:** Pre-write wrong config path → verify registry-derived path wins → verify wrong path NOT written. Required for all registry-first resolution tests.

## Non-Blocking Notes

1. **Idempotency key (ref-name vs SHA):** Spec says filter by `foldedRefs[].sha`. Templates use `.[].foldedRefs[].ref` (ref-name). Matches ref-membership-fold-idempotency skill (team canon). Supersedes spec wording.
2. **Pipeline bash logic untested:** Clock skew, malformed-JSON abort, prune, orphan-branch creation are correct in template but not exercised by unit tests. Inherent limitation of template unit tests (no bash execution environment). Acceptable.
3. **`--force-with-lease` raw check:** Could be fooled by comment. Flag confirmed in push command. Acceptable.

## Outcome

**APPROVE** — 27/27 new tests GREEN. 12/12 piece-34 regression GREEN. Verified no-pr/pull_request assertions, single-writer comment presence, installer idempotency/conflict/missing-dir, registry-first D7, --force-with-lease, permissions.contents=write, no allowScripts. Ready for Phase C.

---

*Logged by Scribe at 2026-06-07T06:44:08Z*
