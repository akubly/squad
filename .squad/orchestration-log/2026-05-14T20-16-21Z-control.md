# CONTROL — Orchestration Log 2026-05-14T20:16:21Z

## Piece 08a: Migrate Read-Only Commands

**Status:** Complete  
**Branch:** `akubly/upstream-08a-migrate-readonly-commands` @ fcb0cf1a  
**Session role:** TypeScript Engineer (CONTROL)

### Files Produced

1. `.changeset/migrate-readonly-commands.md` (patch)
2. `packages/squad-cli/src/cli-entry.ts` (modified)
3. `packages/squad-cli/src/cli/commands/config.ts` (new)
4. `packages/squad-cli/src/cli/commands/cross-squad.ts` (new)
5. `test/cli/legacy-resolver-migration.test.ts` (new)
6. `.squad/agents/control/history.md` (append)
7. `.squad/decisions/inbox/control-08a-working-tree-drift.md` (new, merged to decisions.md by Scribe)

### Test Results

- **In-scope tests:** 5/5 pass (migration tests)
- **Build:** Clean
- **Scrub gate:**
  - Gate 1: FAIL (pre-existing baseline contamination — not piece-08a files)
  - Gate 2: PASS
  - Gate 3: WARN (pre-existing baseline content — not piece-08a files)
  - Gates 4/5/6: PASS

### Key Decisions

1. **Read-only delegation pattern:** `config` and `cross-squad` commands now delegate to cross-squad resolver, establishing REPLAY-PROTOCOL semantics for Phase C upstream integration.
2. **Legacy fallback preserved:** Inline resolution still functional for backward compatibility.
3. **Scrub gate attribution:** Both Gate 1 failure and Gate 3 warning are pre-existing baseline issues, not caused by piece-08a code changes. Piece-diff scope verified independently.

### Coordinator Verdict

**akubly (Brady):** ACCEPT — Piece 08a complete. Scrub gate failures are pre-existing upstream baseline contamination, not piece-08a responsibility. No PR opened per Phase B protocol.

### Working-Tree State

Pre-existing working-tree changes (version stamps, generated template copies, skill directories) were preserved in local stash before creating piece branch. Piece 08a starts from clean predecessor tip.

### Ready for Phase C

Piece 08a is ready for Phase C upstream extraction. No revision cycle required.
