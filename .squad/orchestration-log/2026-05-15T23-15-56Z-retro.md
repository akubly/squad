# 2026-05-15 — RETRO (Security) — Orchestration Log

**Agent:** RETRO  
**Mode:** Adversarial Guard Review  
**Model:** claude-sonnet-4.6  
**Date:** 2026-05-15

## Task Summary

Conducted adversarial security/guard review of piece 10 (Init Fail-Fast Guards) on branch `akubly/upstream-10-init-fail-fast` @ original commit + revision.

## Files Authorized

- `packages/squad-cli/src/commands/init.ts`
- `packages/squad-sdk/src/resolution-v2.ts`
- `test/cli/init-fail-fast.test.ts`

## Files Produced

None. Review-only input. Guard findings recorded in `.squad/decisions.md`.

## Outcome

**APPROVE-WITH-FIXES** (conditional approval pending revision)

**Initial Findings:**
1. Registry conflict checks gated on explicit callsign/registry-path flags only
   - Missing: conflicts when registry comes from `SQUAD_REGISTRY_PATH` or default
   - **FIX:** Treat registry lookup as part of all registering init paths unless `--no-register`
   
2. `.squad` symlink detection follows symlinks, not lstat
   - Missing: sentinel block on symlink without valid guard file
   - **FIX:** Use lstat to reject or flag `.squad` symlinks before scaffold writes

**Post-Revision Status (CONTROL):**
- Registry resolution now unified through one path (`packages/squad-cli/src/commands/init.ts`)
- lstat sentinel added: `.squad` symlinks without guard files are rejected or conflicted
- Both registered and unregistered init paths share the same validation sequence
- All conflict paths block scaffold creation
- Exit code 2 consistently applied to conflicts

## Final State

All must-fix guards resolved. Symlink lstat pattern blocks redirect-escape. Registry path unified. Approved for Phase C.

---
