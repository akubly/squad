# Flight Orchestration Log — Piece 28 Review

**Session:** Phase B replay — piece 28 (Inbox branch publish flow)  
**Date:** 2026-06-02T10:37:49Z  
**Agent:** Flight (Lead)  
**Mode:** sync  
**Routing:** Assigned piece 28 review from `.squad/routing.md` (Lead gate authority post-EECOM impl)

## Work Authorized

Scrub-gate review of piece 28 commit `d42f4e21` against spec `28-inbox-branch-publish-flow.md` and REPLAY-PROTOCOL Phase C scrub criteria. Verify:
- All three helpers exported + properly typed
- Idempotency contract (hydrateTeamRootFromStateRef early exit)
- `.squad/` scope enforcement (hydrateWorkRootProjection)
- Pre-branch alias validation + format
- Snapshot payload allowlist
- `publish-metadata.json` schema (5 fields, no raw paths)
- `pathHash` normalization
- Test coverage (6 spec assertions)
- Build baseline (pre-existing TypeScript errors acceptable)
- Co-authored-by trailer

## Files Authorized for Review

**Diff scope (piece 28~1..28):**
- `packages/squad-sdk/src/sync.ts`
- `packages/squad-sdk/src/index.ts`
- `test/cli/cross-repo-sync.test.ts`
- `.changeset/inbox-branch-publish-flow.md`

**Scrub gate check:** 0 strip-listed paths in diff-scoped commit. Gate 1 baseline interpretation holds per REPLAY-PROTOCOL §Phase C.

## Outcome

**Verdict:** ✅ APPROVED WITH FOLLOW-UP (non-blocking)

**Passing items:** 20/22 explicit spec assertions  
- ✅ All three helpers exported and working
- ✅ Idempotency working (early return on sha match)
- ✅ Write-through `.squad/`-scoped, vanished files removed
- ✅ Pre-branch alias validation (`^[a-z][a-z0-9-]{0,38}$`)
- ✅ Snapshot payload allowlist matches spec
- ✅ `sessionShardPath` format correct
- ✅ Metadata schema (5 fields: repo, pathHash, publishedAt, developerAlias, inboxBranch)
- ✅ `sourceWorkRoot` is `{repo, pathHash}` object, never raw string
- ✅ `pathHash` normalization (lowercase, forward-slash, no trailing slash, `sha256:` prefix)
- ✅ `publishedAt` UTC with Z suffix
- ✅ `developerAlias` regex match
- ✅ No git config user.email capture (synthetic `@squad.local`)
- ✅ 15 tests in cross-repo-sync.test.ts; all 6 spec assertions covered
- ✅ `npm test -- test/cli/cross-repo-sync.test.ts` 15/15 pass
- ✅ `npm run build` — pre-existing baseline (no new TypeScript errors introduced)
- ✅ `npm test` — no regressions (23 failing files baseline maintained)
- ✅ Single squashed commit
- ✅ Co-authored-by trailer present
- ✅ Tone & Record clean (no PII, fork residue, porting language)
- ✅ Branch pushed to origin

**Non-blocking follow-ups (logged to decisions.md):**
1. Commit body completeness — test file and concurrent-publish fixture not explicitly named (cosmetic; visible in --stat)
2. `publishTeamRootToInbox` 4-param signature — spec abbreviates to 3 params; implementation correctly extends to `(teamRoot, remote, inboxBranch, opts: InboxPublishOpts)`. Pragmatically required. **Piece 30 integration MUST use 4-param form.**
3. Build baseline — pre-existing TypeScript errors from SDK version mismatch (piece-27 carryover). Not introduced here. SDK alignment deferred to broader stack work.

**Status:** APPROVED — Ready for merge (subject to Coordinator decision)
