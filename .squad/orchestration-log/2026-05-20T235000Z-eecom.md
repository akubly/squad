# Orchestration: Piece 21 — Post-Stack Adversarial Review Fixes

**Date:** 2026-05-20T23:50:00Z  
**Requested by:** Aaron Kubly  
**Agent:** EECOM (Core Dev)  
**Mode:** Sync  
**Model:** claude-sonnet-4.6  

## Why EECOM

Core Dev owns CLI commands and status command migrations. Piece 21 revises `status` command output and updates test fixtures after adversarial review of the post-stack-review changes.

## Authorized Changes

**Files authorized to read/modify:**
- `packages/squad-cli/src/cli-entry.ts` — status command dispatch
- `packages/squad-cli/src/commands/status.ts` — status command implementation
- `packages/squad-cli/src/commands/__tests__/status.test.ts` — unit tests
- `test/cli/legacy-resolver-migration.test.ts` — integration tests
- `.changeset/post-stack-review.md` — changeset documentation

## Files Produced in Final Code Commit

**Code commit (ed9f81fb):**
1. `.changeset/post-stack-review.md` — changeset for patch version bump
2. `packages/squad-cli/src/cli-entry.ts` — status command dispatch (revised)
3. `packages/squad-cli/src/commands/status.ts` — status command output (revised)
4. `packages/squad-cli/src/commands/__tests__/status.test.ts` — unit tests (expanded)
5. `test/cli/legacy-resolver-migration.test.ts` — integration tests (updated)

**Total files in code commit: 5**

## Coordinator Correction

The Coordinator (akubly) corrected the initial submission to remove 4 out-of-scope files + 1 protocol violation:

1. **`.github/agents/squad.agent.md`** — version stamp updated in the original commit. Reverted: not part of piece 21 scope.
2. **`package.json`** and **`packages/squad-cli/package.json`** — version regressions from build re-stamping. Reverted: not part of piece 21 scope.
3. **`test-fixtures/init-test/.gitignore`** — deletion included in original commit. Reverted: pre-existing file, not in piece scope.
4. **`.squad/agents/eecom/history.md`** — agent history merged into code commit (protocol violation). Reverted: state commit must be separate per REPLAY-PROTOCOL.

**Result:** Original submission had 9 files; final clean code commit has 5 files.

## Outcome Notes

- **Sub-proposal triage:** A,B,C,D,F,H = verified-already-green | E = accept (implemented) | G = verified-noop | I = partial (both items marked as EBUSY-retry-only dropped after verify-first showed they don't reproduce)
- **Tests:** piece-21-relevant suite = 41/41 green. Full suite has 25 pre-existing failures across unrelated domains (acceptance, consult, register, scheduler) — verified pre-existing on predecessor babdfb68; piece 21 introduces zero regressions.
- **Scrub gate:** Gates 2, 5, 6 PASS. Gates 3, 4 WARN (only .squad/ state hits, scribe-managed). Gate 1 FAIL is pre-existing baseline contamination — piece 21 introduces ZERO new strip-list violations.
- **Branch:** akubly/upstream-21-post-stack-review @ ed9f81fb (final code commit after coordinator correction)

## Result

**Phase B Complete for Piece 21.** Code commit verified ready for Phase C (upstream PR).
