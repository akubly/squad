# Orchestration Log — handbook (Handbook)

**Date:** 2026-06-02T23:19:38-07:00  
**Agent:** handbook (Handbook, Documentation & Quality)  
**Mode:** background  
**Status:** success ⚠️ with critical findings

## Session Summary

Handbook spawned in background mode to author local-dogfood runbook for pieces 26–30 and validate implementation against spec via static code analysis before bug bash execution.

## Output

**local-dogfood-runbook.md** — 806 lines, 4 scenarios, 32+ bug-bash probes documented. Covers:
- Single-repo setup validation (piece 26: `squad bind` with self-reference)
- Cross-repo bind workflow (piece 26: external team sidecar)
- Sync flow end-to-end (pieces 27–28: hydration, publish, fold)
- ADO pipeline integration (piece 30: template instantiation, credential safety)

## Critical Finding: 5 Shipping Defects Surfaced

During runbook authorship (code read of bind.ts, sync.ts, cli-entry.ts), Handbook identified **5 real shipping-level defects** via static analysis:

### S0 (Critical) 
- **publishTeamRootToInbox** is exported but never wired to CLI dispatch. Piece 28's inbox publish flow is dead code. No `squad/inbox/*` refs created on `squad sync --push`.

### S1 (High) — 3 defects
1. **stateBackend not written** by `squad bind`. `runSync` detects null backend and bails with "local sync" message.
2. **hydrateTeamRootFromStateRef never called** from CLI. Sidecar remains empty after `squad sync --pull`.
3. **SQUAD_DEVELOPER_ALIAS env var ignored** in resolution chain. Pipeline runs without explicit `--developer` fail silently.

### S2 (Medium)
- **developerAlias not validated** at bind time. Invalid aliases (e.g., `DEV_1`) written to config.json, caught later only if `publishTeamRootToInbox` runs (which it doesn't — see S0).

## Recorded File

**SQUAD_DIR/decisions/inbox/handbook-dogfood-findings.md** — Full details for all 5 findings with file:line citations, reproduction steps, and suggested fixes.

## Assessment

These are not test-gap findings or nits. They represent **incomplete CLI wiring** between pieces 26–28 despite unit-level tests passing. Pieces 27 and 28 function correctly internally; they are not connected to the CLI dispatch layer that coordinates them into a cross-repo workflow.

The defects become production-critical in Phase C (first live ops team dogfood) if not fixed before this commit lands upstream.

## History Update

Handbook history.md appended with:
- Dogfood scenario patterns (single-repo, cross-repo, ADO integration)
- Spec-vs-code divergence analysis (5 critical gaps)
- Static-analysis-first methodology learnings
- Test-vs-runtime validation disconnect

No operational blockers in the runbook itself (it documents theoretical workflows). The critical blockers are the 5 wiring defects in pieces 26–28.
