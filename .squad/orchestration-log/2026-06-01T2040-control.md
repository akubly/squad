# CONTROL — Piece 27 Adversarial Review

**Timestamp:** 2026-06-01T20:40:00Z  
**Reviewer:** CONTROL (TypeScript Engineer)  
**Piece:** 27 — Explicit Sync Command  
**Branch:** squad/piece-27-explicit-sync-command  
**Commit:** 31177e72

## Verdict: ⚠️ APPROVE-WITH-NITS

## Summary

Type design is sound. `SyncOptions.direction` correctly typed as literal union. `SyncGitOps` injection interface clean. Remote resolution precedence chain is pure and nullish-correct. ESM correctness validated. Zero new tsc errors vs. parent (43 pre-existing on both). Zero new suppressions.

## Mandatory Nits

**N1: Untyped config reads bypass SDK interface**
- `readStateRemoteFromConfig` and `readDeveloperAliasFromConfig` hand-roll `JSON.parse(raw)` → `any`
- Should use SDK's typed `loadDirConfig(): SquadDirConfig | null`
- Currently: field-name renames fail at runtime only
- Fix: Replace both functions with single `loadDirConfig()` call, read `config?.stateRemote` and `config?.developerAlias` from typed result

## Non-Blocking Nits

- N2: `SyncOptions.developer?: string` accepts `''` — type system does not enforce non-empty; JSDoc or `NonEmptyString` brand recommended
- N3: `REQUIRED_REFSPECS` named as constant but is a factory function; rename to `requiredRefspecs()` or `getRequiredRefspecs()`
- N4: `return config.stateRemote || undefined` uses falsy-OR; should be `?? undefined` (nullish)
- N5: Sequential `if` for direction flags (last-match-wins); `squad sync --pull --push` → `push` undocumented

## Build/tsc Status

**FAIL (pre-existing)** — 43 errors on piece-26 and piece-27 (zero new errors)  
Baseline: stale published SDK in `node_modules` lacks recent fields

## Suppressions Introduced

0 — none added. Pre-existing `(err as any).stderr` in `syncPush` predates this piece

## Public API Surface

All additive, no removals. `SyncOptions.direction` expanded to include `'hydrate-only'` and `'publish-only'` (no breakage).

## Cross-Piece Consistency

`stateRemote` in `SquadDirConfig` present and typed as `string | undefined` in SDK. Piece-27 aligns with piece-26 design. Hand-rolled read (untyped) is the only gap — N1 fixes this.

## Recommendation

If N1 blocks post-revision: recommend EECOM (implementer). Flight locked out per protocol.
