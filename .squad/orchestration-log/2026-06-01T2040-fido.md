# FIDO — Piece 27 Adversarial Review

**Timestamp:** 2026-06-01T20:40:00Z  
**Reviewer:** FIDO (Quality Owner)  
**Piece:** 27 — Explicit Sync Command  
**Branch:** squad/piece-27-explicit-sync-command  
**Commit:** 31177e72

## Verdict: ⚠️ APPROVE-WITH-NITS

## Summary

All 18 sync-command tests and 15 install-hooks tests pass GREEN. Recursion guard and spec functionality confirmed sound. Two functional defects found: whitespace-alias bypass (N1) and force-install duplication (N2). Neither reflects spec misread; both are implementation bugs with real user impact.

## Mandatory Nits

**N1: Whitespace-only alias bypasses empty-alias guard**
- `runSync()` checks `if (!alias)` — falsy check
- `developer: ' '` (space) passes through; git fails with cryptic ref-name error
- Fix: `if (!alias || !alias.trim())`
- Tests required: `developer: ' '`, `developer: '\t'`

**N2: `installHook()` force-reinstall duplicates squad section**
- Force branch computes `cleaned` variable but never uses it
- Dead code — execution falls through to append, duplicating hook on each `--force` invocation
- Hook can exceed shell limits and cause double-invocations
- Fix: use `content` directly when `force: true` and marker found, remove old section first

## Non-Blocking Nits

- N3: No flag-combination-precedence tests (`--pull --push` → `push`, undocumented)
- N4: `--remote ""` silently ignored with no warning
- N5: No test for `stateRemote` absent + `--both` direction

## Baseline Validation

- Build: identical TS errors on piece-26 and piece-27 (43 on both, zero new)
- Wiring: pre-existing failures (`doctor-types.ts`, `init-remote.ts`); `sync.ts` now wired
- Lint: no piece-27 files appear in failure output

## Hook Recursion Guard: PASS

- All 4 templates contain `SQUAD_SYNC_ACTIVE`
- `runSync()` checks and sets environment variable before git calls
- All recursion tests pass GREEN

## Spec Conformance

17/19 acceptance criteria pass  
2 partial: empty-alias guard, force-reinstall implementation

## Recommendation

If N1 or N2 block post-revision: recommend EECOM (implementer of piece 27). Flight is locked out as original implementer.
